name: AutoClicker32 Release
on:
  push:
    branches: [ master ]
jobs:
  build:
    name: Build AutoClicker32
    runs-on: windows-2019
    steps:
      - name: Create Timestamp
        id: create_timestamp
        run: $buildTime = Get-Date -Format "yyyy-MM-dd_HH-mm-ss"; Write-Host "::set-output name=release_datestamp::$buildTime"
        
#https://github.com/marketplace/actions/checkout
      - name: Check out
        uses: actions/checkout@v2

#https://github.com/marketplace/actions/setup-msbuild          
      - name: Add msbuild to PATH
        uses: microsoft/setup-msbuild@v1.0.2

      - name: Compile x86
        shell: cmd
        run: msbuild AutoClicker32.sln -p:Configuration=Release -p:Platform=x86

      - name: Create Zip
        shell: powershell
        run: |
          Rename-Item -Path LICENSE -NewName license.txt
          $compress = @{
            Path = "Release/AutoClicker32.exe", "license.txt"
            DestinationPath = "AutoClicker32.zip"
            Force = $True
          }
          Compress-Archive @compress

#https://github.com/marketplace/actions/create-a-release        
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.create_timestamp.outputs.release_datestamp }}
          release_name: Release ${{ steps.create_timestamp.outputs.release_datestamp }}
          body: |
            Release from master branch
          draft: false
          prerelease: false

#https://github.com/marketplace/actions/upload-a-release-asset
      - name: Upload Release
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./AutoClicker32.zip
          asset_name: AutoClicker32-${{ steps.create_timestamp.outputs.release_datestamp }}.zip
          asset_content_type: application/zip
